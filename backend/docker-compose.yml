services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: homeopath
      POSTGRES_PASSWORD: homeopath
      POSTGRES_DB: homeopath_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
